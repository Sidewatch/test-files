# gradle.properties — the JDK's rules: three separators, comments, continuations, escapes
! also a comment
org.gradle.jvmargs=-Xmx2g -XX:+UseParallelGC -Dfile.encoding=UTF-8
org.gradle.parallel = true
org.gradle.caching: true
workers 4

# Project
group=com.example
version=1.4.0

# A continuation line, an escaped separator in a key, unicode and tab escapes
welcome.message=Welcome to the inventory service, \
    nothing here is billable\!
key\ with\ spaces = spaced
path=C:\\Users\\app
unicode=caf\u00E9
tab=a\tb
empty=
