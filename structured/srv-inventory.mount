[Unit]
Description=Inventory data volume
After=network-online.target

[Mount]
What=/dev/disk/by-label/inventory
Where=/srv/inventory
Type=ext4
Options=defaults,noatime

[Install]
WantedBy=multi-user.target
