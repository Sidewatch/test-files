# Example schemas

The same data model written three ways — Prisma, a Rails `schema.rb`, Django models — for the
ER diagram (View ▸ Preview on any of them) and the structural schema diff (edit one, then look
at the Changes rail's badge and the file's banner in All Changes).
